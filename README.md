📦 WordPress WooCommerce Bulk Size Guide Updater
یک ابزار کاربردی و سبک برای مدیریت محتوای محصولات ووکامرس. این اسکریپت به صورت خودکار تصاویر راهنمای سایز موجود در توضیحات کوتاه محصول را شناسایی کرده و متن‌های متناظر، جداول یا توضیحات تکمیلی را با استایل استاندارد به محصول اضافه می‌کند.

🚀 ویژگی‌های کلیدی
تشخیص هوشمند تصویر: استخراج نام فایل‌های تصاویر موجود در چکیده محصول (post_excerpt).

پاکسازی خودکار: جلوگیری از تکرار محتوا با حذف بلوک‌های قدیمی پیش از درج محتوای جدید.

گزارش‌دهی پیشرفته (GUI): دارای داشبورد اختصاصی پس از اجرا برای نمایش وضعیت (تعداد موفق، خطاها و لاگ لحظه‌ای).

امنیت بالا: محدود شده به سطح دسترسی مدیریت (manage_options).

طراحی مدرن: خروجی نهایی در سمت سایت با استایل‌های CSS بهینه و فونت فارسی.

🛠 نحوه نصب و راه‌اندازی
۱. افزودن به وردپرس
محتویات فایل را در فایل functions.php قالب خود یا در یک پلاگین اختصاصی قرار دهید.

۲. پیکربندی (Configuration)
در داخل تابع run_update_products_with_report دو بخش اصلی را شخصی‌سازی کنید:

آرایه $product_ids: آی‌دی محصولاتی که قصد آپدیت آن‌ها را دارید وارد کنید.

آرایه $size_text_map: ```php 'file-name.webp' => 'متن یا HTML مورد نظر شما'


۳. اجرا
در حالی که به عنوان مدیر وارد پیشخوان وردپرس هستید، آدرس زیر را در مرورگر باز کنید: https://your-domain.com/wp-admin/admin-post.php?action=update_products

📝 ساختار فنی (Technical Overview)
نحوه عملکرد (Logic Flow)
بررسی سطح دسترسی: ابتدا چک می‌شود که کاربر حتماً مدیر سایت باشد.

استخراج داده: با استفاده از preg_match_all تمامی تصاویر داخل متن شناسایی می‌شوند.

تطبیق داده: نام فایل تصویر (basename) با نقشه (Map) تعریف شده مقایسه می‌شود.

به‌روزرسانی دیتابیس: از تابع wp_update_post برای تزریق محتوای جدید استفاده می‌شود.

محاسبه زمان: مدت زمان اجرای اسکریپت برای مانیتورینگ کارایی (Performance) محاسبه و نمایش داده می‌شود.

استایل‌های پیش‌فرض
اسکریپت به صورت خودکار کلاس .size-guide را به بلوک‌های متنی اختصاص می‌دهد. شما می‌توانید با افزودن کدهای CSS زیر به قالب خود، ظاهر آن را سفارشی کنید:

CSS

.size-guide {
    border: 1px solid #eee;
    padding: 15px;
    border-radius: 8px;
    line-height: 2;
    background-color: #fafafa;
}
📊 نمونه گزارش اجرا (Terminal-like Report)
پس از اجرا، جدولی مشابه زیر مشاهده خواهید کرد:

کل محصولات: ۱۰ مورد

✅ موفق: ۸ مورد

⚠️ اسکیپ شده: ۱ مورد (تصویر ست نشده)

❌ خطا: ۱ مورد (توضیحات کوتاه خالی)

🤝 مشارکت
اگر پیشنهادی برای بهبود اسکریپت (مثلاً افزودن قابلیت انتخاب دسته‌بندی محصولات به جای آی‌دی) دارید، خوشحال می‌شوم یک Pull Request ثبت کنید یا در بخش Issues مطرح کنید.

⚠️ سلب مسئولیت
این اسکریپت مستقیماً دیتابیس محصولات را تغییر می‌دهد. توصیه می‌شود قبل از اولین اجرا در سایت اصلی، حتماً روی یک نسخه Local یا Stage تست شده و از دیتابیس خود Back-up تهیه کنید.